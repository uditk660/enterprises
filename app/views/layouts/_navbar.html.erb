<nav class="pos-navbar no-print">
  <div class="pos-navbar-inner">

    <!-- LEFT: BRAND -->
    <div class="pos-brand">
      <div class="pos-logo">₹</div>
      <div class="pos-brand-text">
        <div class="title">Billing Pro</div>
        <div class="subtitle">Invoice & POS System</div>
      </div>
    </div>

    <!-- CENTER: NAV LINKS -->
    <% if user_signed_in? %>
      <div class="pos-nav-links">
        <%= link_to "Dashboard", root_path, class: "nav-link active" %>
        <%= link_to "Employees", company_employees_path(current_user.companies.first), class: "nav-link" %> 
        <%= link_to "Companies", companies_path, class: "nav-link" %>
        <%= link_to "Customers", customers_path, class: "nav-link" %>
        <%= link_to "Invoices", invoices_path, class: "nav-link" %>
      </div>

      <!-- RIGHT: USER -->
      <div class="pos-user">
        <div class="user-info">
          <span class="name"><%= current_user.email %></span>
          <span class="role">Admin</span>
        </div>

        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "logout-btn" %>
      </div>
      <% else %>
        <div class="pos-auth">

          <%= link_to new_user_session_path, class: "auth-btn login" do %>
            <span class="auth-text">Login</span>
          <% end %>

          <%= link_to new_user_registration_path, class: "auth-btn signup" do %>
            <span class="auth-text">Sign Up</span>
          <% end %>

        </div>
      <% end %>
  </div>
</nav>